function [steerAngle, velocity, WPn] = THINK_findPayload(WPn)
   addpath functions/SENSE
   addpath functions/ACT   
   
   % Looks for payload to pickup (stretch goal, not needed)
   steerAngle = 0;
   velocity = 0;
end